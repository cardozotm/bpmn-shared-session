# Optional server environment (copy to server/.env — gitignored)

# Supabase persistence (service role only)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# MCP / agent HTTP API (Bearer token for /agent/*)
BPMN_AGENT_TOKEN=dev-agent-token
